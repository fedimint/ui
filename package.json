{
  "private": true,
  "scripts": {
    "dev": "mprocs -c ./mprocs.yml",
    "dev:gateway": "./scripts/mprocs-nix-gateway.sh",
    "dev:guardian": "./scripts/mprocs-nix-guardian.sh",
    "build": "turbo run build",
    "build:gateway-ui": "turbo run build --filter=gateway-ui...",
    "build:guardian-ui": "turbo run build --filter=guardian-ui...",
    "clean": "turbo run clean",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "prepare": "husky install"
  },
  "devDependencies": {
    "eslint": "^8.4.1",
    "husky": "^8.0.3",
    "prettier": "^2.5.1",
    "pretty-quick": "^3.1.3",
    "turbo": "latest"
  },
  "packageManager": "yarn@1.22.19",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
